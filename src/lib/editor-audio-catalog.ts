/** Shared catalog keeps planner and manual sound browser on the same licensed files. */
export {
  editorAudioCatalog,
  editorAudioUrl,
  type EditorAudioCatalogEntry,
} from "@contracts/editor-audio-catalog";
